import LetterPullup from "@/registry/components/eldoraui/letter-pullup";

export default async function LetterPullupDemo() {
  return <LetterPullup words={"Staggered Letter Pull Up"} delay={0.05} />;
}
